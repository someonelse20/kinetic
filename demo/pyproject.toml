[build-system]
requires = ["setuptools", "wheel", "pybind11>=2.13"]
build-backend = "setuptools.build_meta"

[project]
name = "kinetic-demo"
version = "0.1.0"
description = "Kinetic EKF library Python bindings demo"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
